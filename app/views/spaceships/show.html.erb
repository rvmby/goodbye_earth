<div class="showcontainer">
<div class="bg-dark text-white text-center">

<div class="container margin-top-negative">
  <h1>Welcome to the homebase of</h1>
  <h1 class="text-align-center"><strong><%= @spaceship.name %>!</strong></h1>
  <p>Here you can find more information and book your journey with: <strong><%= @spaceship.name %></strong> </p>
  <div class="card mb-3">
    <%= cl_image_tag @spaceship.photo.key, height: 300, width: 400, crop: :fill %>
      <div class="card-body text-dark">
      <div class="d-xl-inline-flex">
        <h5 class="card-title"><%= @spaceship.name %></h5>
        <p class="card-text"><%= @spaceship.description %>.</p>
      </div>
      <div class="d-xl-inline-flex">
        <p class="card-text">price: <b><%= @spaceship.price %></b> $</p>
        <p class="card-text"><small class="text-muted">occupancy: <%= @spaceship.max_people %></small></p>
      </div>
      </div>
  </div>
</div>


</div>




  <div>
<% if policy(@spaceship).edit? %>
<%= link_to "Edit this spaceship", edit_spaceship_path(@spaceship) %> |
<% end %>

<%= button_to "Destroy this spaceship", @spaceship, method: :delete if policy(@spaceship).destroy? %>
</div>
</div>

<%#= simple_form_for @booking, data: { controller: 'flatpickr' } do |f| %>
  <%#= f.input :start_time, as: :string, input_html: { data: { flatpickr_target: "startTime" } } %>
  <%#= f.input :end_time, as: :string, input_html: { data: { flatpickr_target: "endTime" } } %>
  <%#= f.submit 'Schedule your appointment' %>
<%# end %>
